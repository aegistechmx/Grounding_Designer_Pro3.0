export const ETAP_CONFIG = {
  gridSize: 70,            // resolución base
  idwPower: 2,             // IDW power
  contourStepMinor: 100,   // 100 V
  contourStepMajor: 500,   // 500 V (thick)
  smoothSegments: 10,      // Catmull-Rom
  smoothTension: 0.5,
  clampIntensity: [0.05, 0.95],
};
